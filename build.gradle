apply plugin: 'java'
apply plugin: 'idea'

defaultTasks 'Client:run'

idea {
  project {
    jdkName = '1.7' 
    languageLevel = '1.7'    
  }
}

subprojects {
  apply plugin: 'java'
  apply plugin: 'idea'
}

